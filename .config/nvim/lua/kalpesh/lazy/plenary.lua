return {
    "nvim-lua/plenary.nvim",
    name = "plenary",     -- Give it a name for reference
    priority = 1000,      -- Load first
    lazy = false,         -- Load immediately on startup
    build = false,
}

